# Define variable
nada = None

# Print variable

print (nada)

# Print variable and your type

print (type(nada))

"""
Entero (int): Representa números enteros sin decimales. Ejemplo: edad = 25.

Flotante (float): Representa números reales con decimales. Ejemplo: precio = 9.99.

Cadena de caracteres (str): Representa una secuencia de caracteres. Ejemplo: nombre = "Juan".

Booleano (bool): Representa un valor de verdadero o falso. Ejemplo: es_mayor = True.

Lista (list): Representa una secuencia mutable de elementos. Ejemplo: numeros = [1, 2, 3, 4, 5].

Tupla (tuple): Representa una secuencia inmutable de elementos. Ejemplo: coordenadas = (10, 20).

Conjunto (set): Representa una colección desordenada de elementos únicos. Ejemplo: frutas = {"manzana", "banana", "naranja"}.

Diccionario (dict): Representa una colección de pares clave-valor. Ejemplo: persona = {"nombre": "Juan", "edad": 25}.

Bytes (bytes): Representa una secuencia inmutable de bytes. Se utiliza para almacenar datos binarios o en formato de bytes. 
Ejemplo: datos = b'\x68\x65\x6c\x6c\x6f' representa la cadena de bytes que corresponde a la palabra "hello".

ByteArray (bytearray): Representa una secuencia mutable de bytes. A diferencia de los bytes, los objetos bytearray pueden modificarse. 
Ejemplo: datos = bytearray(b'\x68\x65\x6c\x6c\x6f') crea un objeto bytearray a partir de una cadena de bytes.

"""

# Conversion de datos

cadena = "David Aguirre"
string = str(41)
print (cadena + " " + string)

flotante = float(41)
entero = int(41)

